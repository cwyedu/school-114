---
title: AI帶你玩程式
sidebar:
  label: AI帶你玩程式
  # Set a custom order for the link (lower numbers are displayed higher up)
  order: 0
---

import Collapsible from '/src/components/Collapsible.astro';

這是彈性學習時間，使用 AI 搭配 turtle graphics 帶您體驗程式設計，有關使用 python 畫圖的資料在 [小烏龜的程式旅程](/turtle-trip/turtle)。

## 第 1 週 基本介紹

- 我常用的 AI 助手：[ChatGPT](https://chat.openai.com/)、[Claude](https://claude.ai/new)、[Gemini](https://gemini.google.com/app)。[怎麼問 AI](/misc/chatgpt-usage)：設定角色、告訴情境、描述需求、限制條件、輸出格式。
- 截圖，抓出基本架構
  ![](https://i.imgur.com/CFlZc6f.png)

<Collapsible title="咒語">
```
你是一位精通 Python Turtle 模組的專家級程式設計師，你有多年教學經驗。
現在說好就好。
```

```
貼上截圖

抓出程式

以後就用這個當基本架構
```

```
做一個像呼吸燈一樣的背景
```

</Collapsible>

---

## 第 2 週 多邊形練習

![](https://i.imgur.com/o626kfk.png)

<Collapsible title="咒語">
```
畫一個 3 角形
```

```
現在我要畫4個多邊形。左下角是3角形，右下角是4邊形，左上角是5邊形，右上角是6邊形
```

你可以在這樣的基礎上畫出什麼樣的圖形呢？試著發揮你的想像力吧！

</Collapsible>

---

## 第 3 週 越來越長

![](https://i.imgur.com/B1nclal.png)

<Collapsible title="咒語">
```
從中心點出發，開始走一段小距離；然後左轉90度，走的距離增加5，然後左轉90度，走的距離增加5，重覆200次
```

**參數** 給我參數

```
這裡的參數，有哪些可以調整，用表格方式列出來並說明
```

這個程式的變形：

- 改變轉的角度
- 改變每次增加的長度
- 改變重覆的次數
- 改變顏色

從這樣的基礎上畫出什麼樣的圖形呢？試著發揮你的想像力吧！

```
從中心點出發，開始走一段小距離；然後左轉90度，走的距離增加5，然後左轉90度，走的距離增加5，重覆
以這個圖為基礎，可以有哪些不一樣的風格變化或變形
```

你可以畫一些列出的風格，但是好像畫不出來，prompt 要寫詳細一點

```
從中心點出發，開始走一段小距離；然後左轉90度，走的距離增加5，然後左轉90度，走的距離增加5，重覆
電路板風格
```

</Collapsible>

## 第 4 週 期中考

## 🐢 第 5 週 模式識別

![](https://i.imgur.com/sJvqMzg.gif)

有沒有發現，這 2 張圖都可以找出相似的模式嗎，我們可以把相似的部分抽出來，變成一個函數 (function)，然後重覆使用這個函數來畫出整張圖。

<Collapsible title="咒語">

左邊的圖：它可以折解成**一支像箭頭的圖**，然後轉一圈重覆畫 60 次。

```
先做一個函數：往前走一大段距離，左轉(b)，往前(a)，退回(a)，右轉(b)，往前(a)，退回(a)，右轉(b)，往前(a)，退回(a)，左轉(b)，退回一大段到原來的位置。

然後重覆畫 60 次，每次右轉 6 度
```

這裡最有可能發生的錯誤是：

- 方向轉錯 (最前面的三個小線段畫完後，方向沒轉回來)
- 距離錯
- 函數沒有正確回到原來的位置

右邊的圖：

```
先做一個函數：畫一個三角形並填滿顏色，再畫一個小點的三角形並填滿顏色，再畫個更小的三角形並填滿顏色。

然後重覆畫 5 次，每次右轉 72 度
```

</Collapsible>

---

## 第 6 週 時鐘

![](https://i.imgur.com/i7rA4rH.gif)

<Collapsible title="咒語">

先畫出刻度

```
先做一個函數(畫刻度)：提筆，走一段長距度，畫一小段，退回(長+短)距確回到原來位置。可以設定參數：長距離、短距離、短距離的寬度

畫刻度：重覆畫 60 次，每次右轉 6 度。逢5的倍數，畫長一點，寬一點。
```

再畫框

```
先做一個函數(畫刻度)：提筆，走一段長距度，畫一小段，退回(長+短)距確回到原來位置。可以設定參數：長距離、短距離、短距離的寬度

畫刻度：重覆畫 60 次，每次右轉 6 度。逢5的倍數，畫長一點，寬一點。

做一個畫框的函數：可以設定：半徑、框的粗細、框的顏色
畫框：用畫框的函數，畫2個，內框粗一點，接聽一個細一點的外框
```

再畫出指針
{/\* ```
做一個畫指針的函數，可以設定：長度、寬度、顏色、角度
畫指針：用畫指針的函數，畫 3 個指針，時針、分針、秒針

```*/}

</Collapsible>

## 最後一次

[二林工商 114-1 彈性學習時間-學生回饋單](https://docs.google.com/forms/d/e/1FAIpQLSd-dHwLKXEC8BxlGIHsqBQ6UlB1tqqk6Z6wt12K4KSOmdhuOg/viewform?pli=1)

```
